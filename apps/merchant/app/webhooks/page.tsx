import {api} from "../../lib/api";async function test(){"use server";await api("/v1/webhooks/test",{method:"POST"});}export default async function Webhooks(){const r=await api("/v1/webhooks");return <><div className="eyebrow">Delivery</div><h1>Webhooks</h1><p className="muted">HMAC-signed events with timestamped replay protection and retry logs.</p><form action={test}><button className="button">Send test event</button></form><div className="section"><table className="table"><thead><tr><th>Endpoint</th><th>Events</th><th>Enabled</th></tr></thead><tbody>{(r.data??[]).map((w:any)=><tr key={w.id}><td className="mono">{w.url}</td><td>{(w.events??[]).join(", ")||"All"}</td><td>{String(w.enabled)}</td></tr>)}</tbody></table></div></>}
